var addBinary = function (a, b) {
  a = a.toString();
  b = b.toString();

  let len = a.length > b.length ? a.length - 1 : b.length - 1;

  const sum = [];
  let hand = 0;

  for (len; len >= 0; len--) {
    
  }

  //expect 100

  //0 1 1
  //0 0 1
  //------
  //1 0 0
};

console.log(addBinary(11, 1));
